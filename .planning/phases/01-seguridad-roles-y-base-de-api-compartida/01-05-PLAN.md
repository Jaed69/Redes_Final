---
phase: 01-seguridad-roles-y-base-de-api-compartida
plan: 5
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - agrodroid/scripts/verify-walking-skeleton.sh
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, AUTH-01, AUTH-02, AUTH-03, INFRA-01, ADMIN-02]

must_haves:
  truths:
    - "A fresh `docker compose up` (after `down -v`, forcing the seed to re-run) brings up db/app/web and all 4 seeded users (admin/cliente/monitor/ti) can log in and receive a JWT verified by the single process.env.JWT_SECRET (SEC-01, AUTH-01, INFRA-01)"
    - "POST /empresas with no Authorization header returns 401; the same request with a non-admin (monitor) token returns 403; the same request with an admin token and a valid body returns 201 and is a real row in Postgres (SEC-02, SEC-04)"
    - "In the browser, each of the 4 roles lands on its own home route after login and is redirected away (never rendering foreign content) when navigating directly to another role's URL (AUTH-02, AUTH-03)"
  artifacts:
    - agrodroid/scripts/verify-walking-skeleton.sh
  key_links:
    - "the script's 4 hardcoded credential pairs (email/password/expected rol) must match whatever Plan 01-02 actually seeded — if Plan 01-02's executed SUMMARY records different literal values than this plan assumed at planning time, update the script's credentials to match the SUMMARY, not the other way round"
    - "this plan's positive-path admin POST /empresas call depends on Plan 01-01's requireRole('admin') actually allowing admin through (not just blocking non-admins) — a bug that only rejects and never allows would pass Plan 01-01's own unit-shaped checks but fail here"
---

<objective>
Prove the phase's walking skeleton end-to-end against a freshly-built `docker compose` stack: real JWT auth for all 4 roles from `.env`, real backend authorization (401/403/201 on `POST /empresas`), and real frontend role-routing/guard behavior in the browser — closing the loop that Plans 01-01 through 01-04 built in isolation.

Purpose: this is the phase's own acceptance test. Every ROADMAP.md success criterion for Phase 1 is either directly asserted by this plan's script or manually confirmed by its checkpoint; nothing here changes application code.
Output: `agrodroid/scripts/verify-walking-skeleton.sh` (a reusable, re-runnable proof), plus a human-confirmed pass on the 4-role browser flow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-CONTEXT.md
@.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-SKELETON.md
@.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-02-SUMMARY.md
@.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-04-SUMMARY.md

@agrodroid/docker-compose.yml
@agrodroid/app/controllers/empresa.controller.js
@agrodroid/app/services/empresa.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and run an automated end-to-end backend verification script</name>
  <files>agrodroid/scripts/verify-walking-skeleton.sh</files>
  <read_first>
    .planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-02-SUMMARY.md — the 4 plaintext credential pairs (email/password/role) Plan 01-02 actually seeded; use these exact values, not the ones drafted at planning time, if they differ.
    .planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-04-SUMMARY.md — confirms the frontend role-redirect targets this plan's checkpoint step will exercise.
    agrodroid/app/services/empresa.service.js (lines 30-46) — `crearEmpresa`'s required body shape (`ruc` exactly 11 numeric digits, `nombreEmpresa`, `direccion`) and its duplicate-RUC/duplicate-name rejection, needed to construct a body that will actually insert successfully on a fresh seeded DB rather than fail on an unrelated validation error.
    agrodroid/docker-compose.yml — confirms the 3 service names (`db`, `app`, `web`) and the `app` service's published port (3000) this script polls.
  </read_first>
  <action>
    Create `agrodroid/scripts/verify-walking-skeleton.sh` as an executable bash script (`chmod +x`) with `set -euo pipefail`, structured as a sequence of named checks, each printing a clear PASS/FAIL line and the whole script exiting non-zero on the first failure (do not let one silent failure cascade into misleading later output). Use `node -e` one-liners (Node is already a project dependency, avoiding any new tool requirement like `jq`) to parse JSON response bodies captured from `curl` into shell variables, rather than fragile `grep`/`cut` parsing.

    Sequence:
    1. From the `agrodroid/` directory, run `docker compose down -v` (ignore failure if nothing is up yet) then `docker compose up -d --build`, so the `db` container's `docker-entrypoint-initdb.d` seed script actually re-runs against a fresh volume (a stale, already-initialized volume would silently skip Plan 01-02's seed migration and this whole verification would validate nothing).
    2. Poll `http://localhost:3000/` (or any lightweight existing route) with `curl -sf` in a bounded retry loop (e.g. up to ~20 attempts with a short pause between, not an unbounded wait) until the `app` container responds, then fail loudly if it never comes up.
    3. For each of the 4 seeded users from `01-02-SUMMARY.md` (email, password, expected `rol`): `curl -s -X POST http://localhost:3000/auth/login` with the JSON body, assert HTTP 200, parse the response, assert `usuario.rol` equals the expected role, and store the returned `token` in a shell variable named for that role (e.g. `TOKEN_ADMIN`, `TOKEN_MONITOR`, `TOKEN_CLIENTE`, `TOKEN_TI`). A single shared `JWT_SECRET` verifying all 4 logins through the same `/auth/login` endpoint is the proof for SEC-01/AUTH-01.
    4. `curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/empresas` with a valid JSON body and **no** `Authorization` header — assert the status code is 401 (SEC-02).
    5. Same request, this time with `Authorization: Bearer $TOKEN_MONITOR` — assert the status code is 403 (SEC-04 blocking a non-admin mutation).
    6. Same request, with `Authorization: Bearer $TOKEN_ADMIN` and a body with an 11-digit all-numeric `ruc` not already present in the seed data (per `empresa.service.js`'s validation), a `nombreEmpresa`, and a `direccion` — assert the status code is 201, proving a real authenticated admin mutation actually succeeds end-to-end against Postgres (SEC-04 positive path; also proves INFRA-01, since this step is unreachable if any of the 3 containers failed to start).
    7. Print a final "WALKING SKELETON VERIFIED" line only if every prior step passed.

    Run the script now as part of completing this task (not just author it) — this task is not done until it has actually executed successfully once against a live `docker compose up` stack.
  </action>
  <verify>
    <automated>test -x agrodroid/scripts/verify-walking-skeleton.sh && bash agrodroid/scripts/verify-walking-skeleton.sh</automated>
  </verify>
  <acceptance_criteria>
    The script exists, is executable, and running it against a freshly-rebuilt `docker compose` stack exits 0, printing a pass line for each of the 7 checks above (4 logins, 401, 403, 201).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the 4-role browser flow end-to-end</name>
  <files>none — browser-only verification, no files modified</files>
  <action>No automated action. This checkpoint confirms, in a real browser, the role-routing and guard behavior Plan 01-04 built (login redirects, cross-role URL blocking, session-clear on logout), running against the live backend Task 1 just brought up and proved with curl-level checks — rendering correctness cannot be asserted by a shell script, only observed.</action>
  <what-built>
    The complete frontend role-routing and guard behavior from Plan 01-04 (`RequireRole`, `ComingSoonView`, the fixed `Login.tsx` redirect switch), running against the real backend Task 1 just proved works — this is the one part of the walking skeleton that requires a human's eyes in a browser, since it's about what actually renders, not just what an API returns.
  </what-built>
  <how-to-verify>
    1. Open `http://localhost:5173` (the `web` container from Task 1's `docker compose up`).
    2. Log in with each of the 4 credential pairs from `01-02-SUMMARY.md` (recorded in Task 1's script) one at a time, logging out between each: confirm `admin` lands on `/admin`, `monitor` lands on `/dashboard`, and both `cliente` and `ti` land on `/proximamente`.
    3. While logged in as `monitor`, type `/admin` directly into the browser's URL bar and press enter — confirm you are redirected away (to `/dashboard`) and no Admin panel content ever appears, even briefly.
    4. While logged in as `cliente` (or `ti`), type `/dashboard` and separately `/admin` directly into the URL bar — confirm both redirect to `/proximamente` with no operator/admin content rendered.
    5. With no user logged in (clear `localStorage` or use a private window), navigate directly to `/dashboard`, `/admin`, and `/proximamente` — confirm all three redirect to `/login`.
    6. On the `/proximamente` placeholder (as `cliente` or `ti`), click "Cerrar sesión" — confirm it returns you to `/login` and a subsequent direct navigation to `/proximamente` redirects to `/login` again (session actually cleared).
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe which one failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This verification script → live `docker compose` stack | The script holds the same 4 dev-only seeded credentials as Plan 01-02 in plaintext, purely to drive the proof — no new secret is introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Information Disclosure | `agrodroid/scripts/verify-walking-skeleton.sh` (hardcoded dev credentials) | low | accept | These are the same already-accepted, dev-only seed credentials from Plan 01-02 (not production secrets); committing them a second time in a verification script does not raise the exposure level, since they're already committed in `db/init.sql`'s hashed form and documented in `01-02-SUMMARY.md` |
| T-01-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No new package installs — the script uses `curl`, `node -e`, and `docker compose`, all already required by this project |
</threat_model>

<verification>
1. `bash agrodroid/scripts/verify-walking-skeleton.sh` exits 0 against a freshly rebuilt stack (Task 1).
2. Human confirms all 6 browser checks in Task 2's checkpoint.
3. Together, these two tasks directly exercise all 5 of Phase 1's ROADMAP.md success criteria.
</verification>

<success_criteria>
- All 5 Phase 1 ROADMAP success criteria are demonstrated true against a live, freshly-built stack, not just asserted per-plan.
- The verification is reusable (a committed script), not a one-off manual transcript that can't be re-run before shipping later phases.
</success_criteria>

<output>
Create `.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-05-SUMMARY.md` when done
</output>
