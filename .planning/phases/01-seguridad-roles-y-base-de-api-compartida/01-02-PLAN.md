---
phase: 01-seguridad-roles-y-base-de-api-compartida
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - agrodroid/docker-compose.yml
  - agrodroid/.env
  - agrodroid/.env.example
  - agrodroid/.gitignore
  - agrodroid/db/init.sql
autonomous: true
requirements: [SEC-03, INFRA-01, AUTH-01]

must_haves:
  truths:
    - "docker-compose.yml contains zero plaintext credential values — every DB/JWT value is ${VAR} interpolated from agrodroid/.env"
    - "agrodroid/.env exists with real dev values and is excluded from git by agrodroid/.gitignore; agrodroid/.env.example is committed as a template"
    - "docker-compose.yml's web service block is nested correctly under services: (INFRA-01)"
    - "docker compose up (run from agrodroid/) starts db, app, and web without config errors"
    - "Usuario seed data covers exactly the 4 role codes admin/cliente/monitor/ti, each with a bcrypt-hashed contrasenia that bcrypt.compare can validate (AUTH-01, unblocks D-02's 4-role login test)"
  artifacts:
    - agrodroid/docker-compose.yml
    - agrodroid/.env
    - agrodroid/.env.example
    - agrodroid/.gitignore
    - agrodroid/db/init.sql
  key_links:
    - "docker-compose.yml's JWT_SECRET value must be the same string the app container's auth.middleware.js/auth.service.js (Plan 01-01) read via process.env.JWT_SECRET, or every login fails to verify"
    - "the 4 seeded contrasenia hashes must be bcrypt (cost-10) hashes of known plaintext passwords, matching auth.service.js's bcrypt.compare(contrasenia, usuario.contrasenia) call — a plaintext or wrong-cost hash makes every seeded login fail"
  prohibitions:
    - "agrodroid/.env must never be committed to git (must be listed in agrodroid/.gitignore before it is created)"
    - "no row in the migrated Usuario seed may keep a plaintext contrasenia value"
---

<objective>
Move DB/JWT credentials out of `docker-compose.yml` into a gitignored `.env` (SEC-03), fix the malformed `web` service block so all 3 containers actually start (INFRA-01), and migrate the `Usuario` seed data to the 4 short role codes with real bcrypt-hashed passwords so all 4 role logins are actually testable (AUTH-01, unblocks D-02).

Purpose: this phase's success criteria explicitly require `docker compose up` to work and 4 real role logins to be testable — today neither is true (compose is malformed, seed passwords are plaintext against a bcrypt.compare check).
Output: a working `docker-compose.yml` + `.env`/`.env.example` pair, and an `init.sql` seed that authenticates for all 4 roles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-CONTEXT.md
@.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-PATTERNS.md

@agrodroid/docker-compose.yml
@agrodroid/db/init.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move docker-compose credentials to .env and fix the web service indentation bug</name>
  <files>agrodroid/docker-compose.yml, agrodroid/.env, agrodroid/.env.example, agrodroid/.gitignore</files>
  <read_first>
    agrodroid/docker-compose.yml (full file, 39 lines) — the `web:` block currently sits at document root, a sibling of `services:` instead of nested under it (line 33), and every credential value is a plaintext literal.
  </read_first>
  <action>
    Create `agrodroid/.gitignore` first (it does not exist yet anywhere in this repo) containing at minimum a `.env` entry, so the `.env` file created next is never staged by git.

    Create `agrodroid/.env` with the exact current values relocated (do not invent new secrets — this is a local dev environment, not production; the goal is removing them from git tracking, not rotating them): `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`, `JWT_SECRET` — same key names and same values docker-compose.yml currently hardcodes, just moved into this file.

    Create `agrodroid/.env.example` alongside it, same key names, with obviously-placeholder values (e.g. `changeme_dev_only`) instead of the real ones — this file IS committed and documents which env vars a fresh clone must set.

    Edit `agrodroid/docker-compose.yml`: replace every hardcoded credential value in the `environment:` blocks of both the `db` and `app` services with `${VAR_NAME}` interpolation referencing the same key names now in `.env` (e.g. the Postgres password line becomes an interpolation of `POSTGRES_PASSWORD`, the JWT line becomes an interpolation of `JWT_SECRET`). Docker Compose auto-loads `agrodroid/.env` for this interpolation since it sits next to `docker-compose.yml` — no extra `env_file:` directive is needed. Separately, fix the indentation bug: the `web:` service block (currently 4 lines starting with `build: ./web`, at 0-indent, sibling to `services:`) must be re-indented to 2 spaces so it nests as a third service under `services:`, alongside `db:` and `app:` — do not change its `build`, `container_name`, `ports`, or `depends_on` values, only its indentation level.
  </action>
  <verify>
    <automated>test -f agrodroid/.env && grep -qx '.env' agrodroid/.gitignore && grep -c '\${JWT_SECRET}' agrodroid/docker-compose.yml | grep -qx 1 && grep -c '\${POSTGRES_PASSWORD}' agrodroid/docker-compose.yml | grep -qx 1 && (cd agrodroid && docker compose config >/dev/null 2>&1) && echo OK</automated>
  </verify>
  <acceptance_criteria>
    `docker compose config` (run from `agrodroid/`) parses successfully and shows exactly 3 services (`db`, `app`, `web`) each with resolved, non-empty environment values. `agrodroid/.env` is untracked (`git status` does not list it). `agrodroid/.env.example` is present with placeholder values.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate Usuario seed data to the 4 role codes with bcrypt-hashed passwords</name>
  <files>agrodroid/db/init.sql</files>
  <read_first>
    agrodroid/db/init.sql (lines 124-133 for the Usuario table definition, lines 293-297 for the current seed INSERT, and lines 429-436 for the Notificacion seed that references Usuario_idUsuario 1 and 2 by id — row order/ids must not shift).
  </read_first>
  <action>
    Replace the 3-row `Usuario` seed INSERT (lines 293-297) with a 4-row version. Keep the existing row order and column values for `nombreUsuario`/`correo`/`Empresa_idEmpresa` unchanged (this preserves the existing ids 1/2/3 that `Notificacion`'s seed rows reference by `Usuario_idUsuario`), only changing each row's `contrasenia` to a bcrypt hash and `rol` to a short code, then append a new 4th row:

    Row 1 (`admin`, `admin@agrovina.com`): `rol` → `admin`; `contrasenia` → `$2b$10$jhHbwEekykq1dkI13xXBr.p7uaXihsf05WOALGAl2BJf.TKYNaJNu` (plaintext: `admin123`).
    Row 2 (`supervisor1`, `supervisor1@agrovina.com`): `rol` → `cliente`; `contrasenia` → `$2b$10$ySqy1qY3ywmGBxkH.mO8RuOzEsLosl3e358jC0QfTDqKUb2XtnptW` (plaintext: `clave123`).
    Row 3 (`operador1`, `operador1@agrovina.com`): `rol` → `monitor`; `contrasenia` → `$2b$10$ySqy1qY3ywmGBxkH.mO8RuOzEsLosl3e358jC0QfTDqKUb2XtnptW` (same plaintext `clave123`, reuse the same hash — do not recompute a different one).
    New Row 4 (`ti1`, `ti1@agrovina.com`, `Empresa_idEmpresa` 1): `rol` → `ti`; `contrasenia` → `$2b$10$K1Ek5km.ZiBfAuDlmKmzUObp1iFi1ms0m/.UFdbxBe/X1P46QgvGy` (plaintext: `ti123`).

    These are real bcrypt hashes (cost factor 10, matching `bcrypt.hash(contrasenia, 10)` in `auth.service.js`) already computed and verified against the `bcrypt` package installed in `agrodroid/app/node_modules` — use them verbatim, do not regenerate. Do not reorder the VALUES list and do not change any other seed table in this file. Record the 4 plaintext credential pairs (email/password/role) in your SUMMARY — Plan 01-05 needs them to test all 4 logins end-to-end.
  </action>
  <verify>
    <automated>grep -c '\$2b\$10\$' agrodroid/db/init.sql | grep -qx 4 && grep -c "'admin', 1)\|'cliente', 1)\|'monitor', 1)\|'ti', 1)" agrodroid/db/init.sql | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    `init.sql` seeds exactly 4 `Usuario` rows with roles `admin`, `cliente`, `monitor`, `ti`, each with a `$2b$10$`-prefixed hash as `contrasenia`. Row ids 1-3 keep their original `nombreUsuario`/`correo` so `Notificacion`'s existing `Usuario_idUsuario` references (1, 2) still point at the same logical users.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git repository → anyone with repo/clone access | `docker-compose.yml` is the boundary — anything hardcoded there is effectively public once pushed |
| `db` container init script → Postgres | Seed data written once at first container bring-up via `docker-entrypoint-initdb.d`, trusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | `docker-compose.yml` (DB + JWT credentials) | high | mitigate | Move all credential values to `agrodroid/.env`, gitignored; `docker-compose.yml` only references `${VAR}` names (Task 1) |
| T-01-05 | Information Disclosure / Tampering | `db/init.sql` seed `contrasenia` values | high | mitigate | Replace plaintext seed passwords with bcrypt (cost 10) hashes matching `auth.service.js`'s `bcrypt.compare` check (Task 2) — also fixes a functional bug where seeded logins silently failed |
| T-01-11 | Repudiation (config-only) | `docker-compose.yml` `web` service indentation bug | medium | mitigate | Correct 2-space nesting under `services:` so all 3 containers actually start (Task 1, INFRA-01) — previously `web` silently never started under `docker compose up`, masking the frontend's absence rather than erroring |
| T-01-SC | Tampering (supply chain) | npm/pip installs | n/a | accept | No new package installs in this plan |
</threat_model>

<verification>
1. `cd agrodroid && docker compose config` resolves all 3 services with no missing-variable warnings.
2. `cd agrodroid && docker compose up -d` brings up `db`, `app`, `web` (full end-to-end confirmation happens in Plan 01-05, which depends on this plan).
3. Manual `SELECT nombreusuario, rol FROM usuario;` after a fresh volume bring-up shows exactly 4 rows with the 4 target role codes.
</verification>

<success_criteria>
- SEC-03: no plaintext DB/JWT credential remains in `docker-compose.yml`; `.env` is gitignored, `.env.example` is committed.
- INFRA-01: `docker compose up` starts all 3 services correctly.
- AUTH-01: `Usuario.rol` seed data demonstrates all 4 supported role values with working (bcrypt-hashed) credentials.
</success_criteria>

<output>
Create `.planning/phases/01-seguridad-roles-y-base-de-api-compartida/01-02-SUMMARY.md` when done
</output>
