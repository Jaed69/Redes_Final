version: '3.9'

services:
  api:
    build: ./api
    container_name: agrodroid-api
    ports:
      - "3000:3000"
    environment:
      DB_HOST: host.docker.internal
      DB_PORT: 5432
      DB_NAME: vinedosdb
      DB_USER: admin
      DB_PASSWORD: admin123
      PORT: 3000